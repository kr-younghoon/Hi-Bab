export default function SwitchCamera(props) {
    return (
        <>
            <div>
                <button onClick={props.handleClick}>Switch camera</button>
            </div>
        </>
    );
}
